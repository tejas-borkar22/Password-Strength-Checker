// Utility File for regex

// check if password contains at least one number
export const hasNumber = /\d/;

// check if password contains at least one upper case character
export const hasUpperCase = /[A-Z]/;

// check if password contains at least one lower case character
export const hasLowerCase = /[a-z]/;

// check if password contains at least one special character #not working
export const hasSpecialCharacter = /[!@#$%^&*()\\[\]{}\-_+=~`|:;"'<>,./?]/;


